import { InputLimitValidatorDirective } from './input-limit-validator.directive';

describe('InputLimitValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new InputLimitValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
