import {CurrencyLengthDirective} from './currency-length.directive'

describe('CurrencyLengthDirective', () => {
  it('should create an instance', () => {
    const directive = new CurrencyLengthDirective();
    expect(directive).toBeTruthy();
  });
});