import { TabNavigateDirective } from './tab-navigate.directive';

describe('TabNavigateDirective', () => {
  it('should create an instance', () => {
    const directive = new TabNavigateDirective();
    expect(directive).toBeTruthy();
  });
});
