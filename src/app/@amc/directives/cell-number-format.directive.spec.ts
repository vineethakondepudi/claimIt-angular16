import { CellNumberFormatDirective } from './cell-number-format.directive';

describe('CellNumberFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new CellNumberFormatDirective();
    expect(directive).toBeTruthy();
  });
});
